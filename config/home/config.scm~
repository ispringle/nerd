(use-modules (gnu)
	     (gnu home)
	     (gnu home services)
	     (gnu packages emacs)
	     (gnu services)
	     (guix packages)
	     (guix git-download)
	     (guix build-system emacs)
	     (guix licenses))

(home-environment
 (packages (list git))
 (services (list)))
